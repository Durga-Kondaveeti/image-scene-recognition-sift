# Core
numpy>=1.24
pillow>=10.0
tqdm>=4.66
matplotlib>=3.8

# PyTorch + vision (Mask R-CNN is in torchvision)
torch>=2.1
torchvision>=0.16

# CLIP via Hugging Face
transformers>=4.41
sentencepiece>=0.2.0
accelerate>=0.30

# Optional but useful
scikit-learn>=1.3
opencv-python>=4.8

# Jupyter / notebooks (optional for local runs)
jupyter>=1.0
ipykernel>=6.29

